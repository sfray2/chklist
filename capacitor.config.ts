import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shaenafray.checklist',
  appName: 'chklist',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
